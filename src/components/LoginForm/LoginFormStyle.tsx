import { StyleSheet } from 'react-native';
import { Metrics } from '../../themes';

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  outline: {
    alignSelf: 'center',
    marginTop: Metrics.moderateScale._24
  },
  avoidingViewContainer: {
  }
});
